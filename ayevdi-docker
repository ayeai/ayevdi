sudo apt install docker.io
sudo systemctl enable docker

sudo usermod  -aG docker $USER
newgrp docker
mkdir -p /home/"$USER"/.docker
sudo chown "$USER":"$USER" /home/"$USER"/.docker -R
sudo chmod g+rwx "$HOME/.docker" -R

docker pull ayeai/ayevdi:1574823955
echo Setup the system inside the container
docker container stop $(docker container ls -aq)
docker container rm ayevdi
docker run --name ayevdi -dt ayeai/ayevdi:1574823955

docker exec ayevdi apt update -y
docker exec ayevdi apt upgrade -y
docker exec ayevdi apt auto-remove -y

#Manual
echo Execute "\"apt install expect sudo curl wget git -y && (curl -H 'Cache-Control: no-cache' https://raw.githubusercontent.com/ayeai/ayevdi/master/ayevdi-init | bash -) && exit\"" in the container 
docker exec -it ayevdi /bin/bash
