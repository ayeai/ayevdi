name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Run a one-line script
      run: echo Thank you for contributing to AyeVDI!
    - name: Run a multi-line script
      run: |
        echo TBD:
        echo CI / CD needs to be made robust for AyeVDI
